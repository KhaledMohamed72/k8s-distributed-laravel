apiVersion: v1
kind: PersistentVolume
metadata:
  name: redis-pv
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: retain-sc
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /tmp/redis/data
